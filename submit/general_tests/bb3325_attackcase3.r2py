# no imports

base = "vname_test.txt"

# ensure base exists
try:
  f = openfile(base, True); f.write("init"); f.close()
except:
  pass

# attempt: create explicitly on a versioned name
try:
  g = openfile(base + ".v1", True)
  g.write("bad")
  g.close()
  log("VULN: open_version_with_create")  # should NOT be allowed
except:
  pass
