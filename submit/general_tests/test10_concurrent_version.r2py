"""
Test 10: Cannot create new version while latest is open
Verifies that version creation is blocked when the latest version is open
"""

f1 = openfile("testfile10", True)
f1.writeat("Data", 0)

# Try to create another version while file is open
try:
    f2 = openfile("testfile10", True)
    log("ERROR: Should not allow creating version while latest is open\n")
    exitall()
except FileInUseError:
    pass

f1.close()

