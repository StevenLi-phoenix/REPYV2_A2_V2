# Simple test that should print but won't due to initialization crash
log("TEST START - This should appear but won't\n")

try:
    f = openfile("test.txt", True)
    f.writeat("Hello", 0)
    f.close()
    log("SUCCESS: File created\n")
except Exception as e:
    log("ERROR: " + str(e) + "\n")

log("TEST END\n")

